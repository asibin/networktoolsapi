sudo: required

language: python

python:
  - 2.6
  - 2.7

services:
  - docker

before_install:
  - docker build -t asibin/networktoolsapi .
  - docker run asibin/networktoolsapi
script:
  - python tests/tests.py